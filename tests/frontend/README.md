Frontend test suite (Vitest) will be added starting with the first phase that introduces
non-trivial component logic (P6 Recommendation Card). P0 acceptance only requires
`npm run build` to pass, which is verified in CI and the daily report.
